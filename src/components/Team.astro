---
import Monster from './Monster.astro';
interface Props {
  team: string[];
}
const { team } = Astro.props;
---

<div class="monster-team">
  {team.map((id, index) => (
    <Monster id={id} leader={index === 0} />
  ))}
</div>
